echo "# Artiq" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Jmaes1212/Artiq.git
git push -u origin main
# Artiq Prints Storefront

This repository contains the static storefront prototype for Artiq Prints, including the homepage, category, and product templates along with all supporting assets.

## Downloadable build

A ready-to-use zip bundle is available at [`dist_artiq_prints.zip`](dist_artiq_prints.zip). Download and extract it to get the compiled HTML, CSS, and JavaScript files for deployment or local preview.
